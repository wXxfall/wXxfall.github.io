# Hexo 博客撰写指南

## 目录

- [创建新文章](#创建新文章)
- [文章结构](#文章结构)
- [Markdown 基础语法](#markdown-基础语法)
- [高级功能](#高级功能)
- [发布文章](#发布文章)
- [图片管理](#图片管理)
- [分类和标签](#分类和标签)
- [数学公式](#数学公式)

***

## 创建新文章

### 命令行创建

```bash
# 创建新文章
hexo new "文章标题"

# 创建草稿
hexo new draft "文章标题"

# 创建页面
hexo new page "页面名称"
```

### 文章位置

- **文章**：`source/_posts/` 目录
- **草稿**：`source/_drafts/` 目录
- **页面**：`source/` 目录

***

## 文章结构

### Front Matter（前置元数据）

每篇文章开头必须包含 Front Matter，使用 YAML 格式：

```markdown
---
title: 文章标题
date: 2026-03-26 10:00:00
tags:
  - 标签1
  - 标签2
categories:
  - 分类
---
```

### 常用 Front Matter 字段

| 字段           | 说明    | 示例                               |
| ------------ | ----- | -------------------------------- |
| `title`      | 文章标题  | `title: "我的第一篇文章"`               |
| `date`       | 发布日期  | `date: 2026-03-26 10:00:00`      |
| `updated`    | 更新日期  | `updated: 2026-03-26 15:00:00`   |
| `tags`       | 标签    | `tags: [Hexo, 博客]`               |
| `categories` | 分类    | `categories: 技术`                 |
| `author`     | 作者    | `author: meteoron`               |
| `permalink`  | 自定义链接 | `permalink: /post/my-first-post` |
| `excerpt`    | 摘要    | `excerpt: 这是一篇关于...`             |
| `toc`        | 目录    | `toc: true`                      |
| `comments`   | 评论    | `comments: true`                 |
| `math`         | 数学公式  | `math: true`                       |

### 完整示例

```markdown
---
title: "Hexo 博客入门指南"
date: 2026-03-26 10:00:00
tags:
  - Hexo
  - 博客
  - 教程
categories:
  - 技术教程
author: meteoron
toc: true
comments: true
---

# Hexo 博客入门指南

欢迎来到 Hexo 博客世界！
```

***

## Markdown 基础语法

### 标题

```markdown
# 一级标题
## 二级标题
### 三级标题
#### 四级标题
##### 五级标题
###### 六级标题
```

### 文本样式

```markdown
*斜体*
_斜体_

**粗体**
__粗体__

***粗斜体***
___粗斜体___

~~删除线~~
```

### 列表

#### 无序列表

```markdown
- 项目 1
- 项目 2
  - 子项目 2.1
  - 子项目 2.2
- 项目 3
```

#### 有序列表

```markdown
1. 第一步
2. 第二步
3. 第三步
```

### 链接和图片

```markdown
# 链接
[链接文本](https://example.com)

# 图片
![图片描述](/img/example.png)

# 带标题的图片
![图片描述](/img/example.png "图片标题")
```

### 引用

```markdown
> 这是一段引用
>
> > 嵌套引用
```

### 代码

#### 行内代码

```markdown
使用 `hexo new` 命令创建新文章
```

#### 代码块

```markdown
\`\`\`javascript
function hello() {
  console.log("Hello, World!");
}
\`\`\`
```

#### 指定语言

```markdown
\`\`\`python
def hello():
    print("Hello, World!")
\`\`\`
```

### 表格

```markdown
| 列1 | 列2 | 列3 |
|-----|-----|-----|
| 内容1 | 内容2 | 内容3 |
| 内容4 | 内容5 | 内容6 |
```

### 分隔线

```markdown
---
```

***

## 高级功能

### 提示框

```markdown
:::note
这是一个提示框
:::

:::tip
这是一个技巧提示
:::

:::warning
这是一个警告
:::

:::danger
这是一个危险警告
:::
```

### 脚注

```markdown
这是一段文字[^1]

[^1]: 这是脚注内容
```

### 任务列表

```markdown
- [x] 已完成任务
- [ ] 未完成任务
```

### HTML 标签

```markdown
<div style="color: red;">红色文字</div>

<details>
<summary>点击展开</summary>
这是隐藏内容
</details>
```

***

## 发布文章

### 完整工作流程

#### 步骤 1：创建文章

```bash
# 创建新文章
hexo new "文章标题"
```

#### 步骤 2：编辑文章

```bash
# 编辑文章文件
# 文件位置：source/_posts/文章标题.md
```

#### 步骤 3：本地预览

```bash
# 启动本地服务器
hexo server

# 或使用简写
hexo s

# 指定端口（避免端口冲突）
hexo server -p 4001

# 后台运行（不占用终端）
hexo server -d
```

**访问本地预览**：

- 默认地址：`http://localhost:4000`
- 指定端口：`http://localhost:4001`

**本地预览特点**：

- ✅ 实时预览，修改后刷新即可看到效果
- ✅ 无需网络连接
- ✅ 快速调试和测试
- ✅ 不会影响线上网站

#### 步骤 4：检查和调整

在本地预览中检查：

- 文章内容是否正确
- 图片是否正常显示
- 格式是否美观
- 链接是否有效
- 数学公式是否正确

#### 步骤 5：停止本地服务器

```bash
# 按 Ctrl + C 停止服务器
```

#### 步骤 6：生成静态文件

```bash
# 清除缓存（推荐）
hexo clean

# 生成静态文件
hexo generate

# 或使用简写
hexo g
```

**生成说明**：

- 生成后的文件在 `public/` 目录
- 包含所有静态资源（HTML、CSS、JS、图片等）
- 这些文件将被部署到 GitHub

#### 步骤 7：部署到 GitHub

```bash
# 部署到 GitHub
hexo deploy

# 或使用简写
hexo d
```

**部署说明**：

- 将 `public/` 目录的内容推送到 GitHub
- GitHub Pages 会自动构建和部署
- 首次部署需要 1-5 分钟，后续更新通常 1 分钟内

#### 步骤 8：访问线上网站

部署成功后访问：

- GitHub Pages：`https://wXxfall.github.io`
- 等待 1-2 分钟让部署完成

### 快速发布流程（推荐）

```bash
# 1. 创建文章
hexo new "文章标题"

# 2. 编辑文章（使用文本编辑器）

# 3. 本地预览
hexo server

# 4. 访问 http://localhost:4000 检查效果

# 5. 停止服务器（Ctrl + C）

# 6. 一键发布
hexo clean && hexo generate && hexo deploy
```

### 一键发布脚本

创建 `publish.bat` 文件：

```batch
@echo off
echo 正在发布博客...
cd /d f:\hexo\blog
hexo clean
hexo generate
hexo deploy
echo 发布完成！
pause
```

**使用方法**：

1. 双击运行 `publish.bat`
2. 等待发布完成
3. 访问网站查看更新

### 本地预览详细说明

#### 启动选项

```bash
# 基本启动
hexo server

# 指定端口
hexo server -p 4001

# 静态模式（只生成静态文件，不监视文件变化）
hexo server -s

# 后台运行
hexo server -d

# 调试模式（显示详细日志）
hexo server --debug
```

#### 预览时的热重载

- Hexo 支持热重载，修改文件后自动刷新
- 修改 Markdown 文件后，浏览器会自动刷新
- 修改配置文件后，需要重启服务器

#### 常见预览问题

**问题 1：端口被占用**

```bash
# 错误信息
Error: listen EADDRINUSE: address already in use :::4000

# 解决方案 1：使用其他端口
hexo server -p 4001

# 解决方案 2：关闭占用端口的进程
# 查找占用端口的进程
netstat -ano | findstr :4000

# 关闭进程（替换 PID）
taskkill /PID 进程ID /F
```

**问题 2：修改不生效**

```bash
# 解决方案：清除缓存后重新启动
hexo clean
hexo server
```

**问题 3：图片不显示**

```bash
# 检查图片路径是否正确
# 确保图片在 source/img/ 目录
# 使用绝对路径：/img/example.png
```

### 部署详细说明

#### 部署前检查清单

- [ ] 文章内容已完成
- [ ] 本地预览效果满意
- [ ] 图片路径正确
- [ ] 链接有效
- [ ] 数学公式正确
- [ ] Front Matter 配置正确

#### 部署命令详解

```bash
# 清除缓存
hexo clean
# 删除 db.json 和 public/ 目录

# 生成静态文件
hexo generate
# 根据 source/ 目录生成 public/ 目录

# 部署到 GitHub
hexo deploy
# 将 public/ 目录推送到 GitHub
```

#### 部署后验证

1. **检查部署状态**
   - 访问：<https://github.com/wXxfall/wXxfall.github.io/deployments>
   - 确认状态为 "Success"
2. **访问网站**
   - 等待 1-2 分钟
   - 访问：<https://wXxfall.github.io>
   - 检查更新是否生效
3. **清除浏览器缓存**
   - 按 Ctrl + Shift + R 强制刷新
   - 或使用无痕模式访问

### 工作流程对比

| 步骤 | 本地预览         | 线上发布       |
| -- | ------------ | ---------- |
| 1  | 编辑文章         | 编辑文章       |
| 2  | 启动服务器        | 本地预览检查     |
| 3  | 访问 localhost | 清除缓存       |
| 4  | 检查效果         | 生成静态文件     |
| 5  | 调整修改         | 部署到 GitHub |
| 6  | 重复预览         | 等待部署完成     |
| 7  | -            | 访问线上网站     |

### 最佳实践

#### 1. 始终先本地预览

```bash
# ✅ 推荐流程
hexo new "文章"
编辑文章
hexo server
检查效果
hexo clean && hexo generate && hexo deploy

# ❌ 不推荐流程
hexo new "文章"
编辑文章
hexo deploy
```

#### 2. 使用版本控制

```bash
# 提交源代码到 Git
git add .
git commit -m "更新文章"
git push origin main
```

#### 3. 定期备份

```bash
# 备份整个博客目录
# 包括 source/、themes/、_config.yml 等
```

#### 4. 测试重要更新

```bash
# 对于重要更新，先在本地充分测试
hexo server
# 多次检查，确保无误后再部署
```

### 故障排除

#### 问题：部署失败

**可能原因**：

1. 网络连接问题
2. Git 配置错误
3. GitHub 仓库配置错误

**解决方案**：

```bash
# 1. 检查网络连接
ping github.com

# 2. 检查 Git 配置
git config --list

# 3. 检查仓库配置
git remote -v

# 4. 手动上传（如果自动部署失败）
# 访问 GitHub 仓库手动上传 public/ 目录
```

#### 问题：部署后网站未更新

**解决方案**：

1. 等待 1-2 分钟（GitHub Pages 需要时间）
2. 清除浏览器缓存（Ctrl + Shift + R）
3. 检查部署状态页面
4. 确认部署分支正确

### 本地预览

```bash
# 启动本地服务器
hexo server

# 指定端口
hexo server -p 4001
```

访问：`http://localhost:4000`

### 生成静态文件

```bash
# 清除缓存
hexo clean

# 生成静态文件
hexo generate
```

### 发布到 GitHub

```bash
# 部署到 GitHub
hexo deploy
```

### 一键发布

```bash
hexo clean
hexo generate
hexo deploy
```

***

## 图片管理

### 图片存放位置

```
source/
├── img/
│   ├── avatar.png
│   ├── default.png
│   └── post/
│       └── example.png
```

### 图片引用

```markdown
# 绝对路径（推荐）
![图片](/img/post/example.png)

# 相对路径
![图片](./example.png)
```

### 图片优化

1. **压缩图片**：使用 TinyPNG 等工具压缩
2. **使用 WebP 格式**：更小的文件大小
3. **使用 CDN**：将图片托管到 CDN

***

## 分类和标签

### 分类

```markdown
---
categories:
  - 技术
  - 前端
---
```

### 标签

```markdown
---
tags:
  - Hexo
  - 博客
  - 教程
---
```

### 分类和标签的区别

| 特性 | 分类       | 标签    |
| -- | -------- | ----- |
| 层级 | 有层级关系    | 无层级关系 |
| 数量 | 通常 1-2 个 | 可以多个  |
| 用途 | 文章归类     | 内容标记  |

***

## 数学公式

### 行内公式

```markdown
$E = mc^2$
```

### 块级公式

```markdown
$$
\int_{a}^{b} f(x) dx = F(b) - F(a)
$$
```

### 常用数学符号

```markdown
# 分数
$\frac{a}{b}$

# 上标和下标
$x^2$, $x_1$

# 根号
$\sqrt{x}$

# 求和
$\sum_{i=1}^{n} i$

# 积分
$\int_{a}^{b} f(x) dx$

# 希腊字母
$\alpha$, $\beta$, $\gamma$, $\delta$

# 矩阵
$$
\begin{pmatrix}
a & b \\
c & d
\end{pmatrix}
$$
```

***

## 最佳实践

### 1. 文章命名

- 使用英文或拼音
- 使用连字符分隔单词
- 示例：`hexo-tutorial.md`, `bo-ke-ru-men.md`

### 2. 图片命名

- 使用小写字母
- 使用连字符或下划线
- 示例：`blog-cover.png`, `post_image_01.jpg`

### 3. 代码块

- 始终指定语言
- 添加注释说明
- 保持代码简洁

### 4. 文章结构

- 使用清晰的标题层级
- 添加适当的空行
- 使用列表组织内容

### 5. SEO 优化

- 使用描述性的标题
- 添加关键词
- 使用合适的标签和分类

***

## 常见问题

### Q1: 文章没有显示？

**A**: 检查以下几点：

1. Front Matter 格式是否正确
2. 文件是否在 `source/_posts/` 目录
3. 是否运行了 `hexo generate`

### Q2: 图片不显示？

**A**: 检查以下几点：

1. 图片路径是否正确
2. 图片文件是否存在
3. 图片格式是否支持

### Q3: 数学公式不显示？

**A**: 检查以下几点：

1. 是否启用了数学公式支持
2. 公式语法是否正确
3. 是否安装了相关插件

### Q4: 如何删除文章？

**A**:

1. 删除 `source/_posts/` 中的文章文件
2. 运行 `hexo clean`
3. 运行 `hexo generate`
4. 运行 `hexo deploy`

***

## 快速参考

### 常用命令

```bash
# 创建文章
hexo new "标题"

# 启动服务器
hexo server

# 清除缓存
hexo clean

# 生成静态文件
hexo generate

# 部署
hexo deploy

# 一键发布
hexo clean && hexo generate && hexo deploy
```

### Front Matter 模板

```markdown
---
title: "文章标题"
date: 2026-03-26 10:00:00
tags:
  - 标签
categories:
  - 分类
toc: true
comments: true
---
```

***

## 结语

恭喜你！现在你已经掌握了 Hexo 博客撰写的所有基础知识。开始创作你的第一篇文章吧！

如有问题，请参考 Hexo 官方文档：<https://hexo.io/docs/>
